# Data Sources

**Before implementing data fetches**: Check `/capabilities` → Sources tab (17 providers).

**Celery rule**: NEVER create manual scripts for data pulling. ALWAYS create scheduled Celery tasks.

For detailed patterns, use the `python-patterns` skill.
