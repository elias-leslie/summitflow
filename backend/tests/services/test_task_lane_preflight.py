"""Lane conflict detection tests — split into focused modules.

Tests have been moved to:
- test_task_lane_ownership.py  — ownership/inventory payload tests
- test_task_lane_conflicts.py  — active lane blocking and stale lane tests
- test_task_lane_scopes.py     — scope validation and overlap detection tests
"""
