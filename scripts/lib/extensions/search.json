{
  "id": "code-intelligence.search",
  "owner": "code-intelligence",
  "namespace": "search",
  "version": "0.1.0",
  "st_contract_versions": [
    1
  ],
  "summary": "Precision code search using canonical project indexes and checkout overlays.",
  "effects": [
    "read-local",
    "read-remote",
    "write-remote",
    "network"
  ],
  "help": {
    "": "Usage: st search [OPTIONS] [query]...\n\n  Search codebase symbols, endpoints, and tables with Precision Code Search.\n\nArguments:\n  query...  Search query (symbol name, function, class, endpoint)\n\nOptions:\n  -P, --project <str>             Project ID (overrides auto-detection for\n                                  this search)\n  -b, --budget <int>              Token budget for prompt context  [default:\n                                  1200]\n  -l, --limit <int>               Maximum primitive results  [default: 20]\n  --text                          Use the text/content search primitive\n  --symbols                       Use the symbol search primitive\n  -j, --json                      Emit full JSON payload\n  -f, --file <str>                List all symbols in a specific file\n  --path <str>                    Restrict search to a relative file/subtree\n                                  prefix\n  --hint / --no-hint              Show refinement hints when results are poor\n                                  [default: hint]\n  --scope <auto|project|checkout>\n                                  Search scope: canonical project index,\n                                  current checkout, or auto-detect both when\n                                  needed  [default: auto]\n  --install-completion            Install completion for the current shell.\n  --show-completion               Show completion for the current shell, to\n                                  copy it or customize the installation.\n  --help                          Show this message and exit."
  },
  "usage": [
    {
      "surface": "st.search",
      "tier": "mandate",
      "cmd": "st search \"query\"",
      "when": "repo/code discovery; before adding any function/endpoint/command/component — search first and extend a close match instead of writing new",
      "precautions": [
        "prefer over rg/grep/find/st memory search",
        "best with 1-4 identifier-shaped terms (CamelCase/snake_case/filenames); for prose questions use --text with a literal phrase",
        "if mode=text-fallback or empty, reshape the query (different identifier, --text phrase, or --file/--path) — never retry verbatim; if the hint says a definition was missed, the symbol index is stale — follow the hint (--scope checkout or rescan), do not reshape",
        "on a close match, extend/reuse it rather than adding a near-duplicate"
      ]
    }
  ]
}
